
#include <iostream>
#include <string>
#include <cmath>
#include <vector>
using namespace std;

// int CalcEmpSalary(int salary) {
//     if(!salary) return 0;

//     if(salary < 1500) { // if less than >>
//         return (salary + (salary * 10 / 100)) + (salary * 90 / 100);
//     } else { // if equal or above than >>
//         return (salary + 500) + (salary * 98 / 100);
//     }
// }

int main() {
    string thisFood = "Greek";
    string userFood;
    // cin >> userFood;
    if(1 == 2 || 1==1 && 1==2) {
        cout << "Sure this work :)" << endl;
    } else {
        cout << "Not Work :(" << endl;
    }


    return 0;
}